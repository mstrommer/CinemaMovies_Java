package oop.polymorph.solution;

import java.util.Date;

/**
 * The CinemaMovie3D class represents a 3D movie.
 * It extends the CinemaMovieBase class.
 */
public class CinemaMovie3D extends CinemaMovieBase {
    /**
     * Constructs a CinemaMovie3D object with the specified start date, title, and
     * genre.
     * 
     * @param startDate The start date of the movie.
     * @param title     The title of the movie.
     * @param genre     The genre of the movie.
     */
    public CinemaMovie3D(Date startDate, String title, String genre) {
        
    }

    /**
     * Returns a teaser message for the 3D movie.
     * Overrides the displayTeaser method in the parent class.
     * 
     * @return A teaser message for the 3D movie.
     */
    @Override
    public String displayTeaser() {
        
    }
}
